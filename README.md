# To-do-list
To-Do List application built using Python, demonstrating the use of lists (arrays), CRUD operations, and basic state management. The application allows users to add, view, update, delete, and mark tasks as completed through an interactive menu-driven interface. This project highlights core Python concepts, user input handling, and console-based UI updates.
